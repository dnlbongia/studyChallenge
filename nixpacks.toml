[phases.setup]
nixPkgs = ["nodejs_22", "npm-11_x"]

[phases.install]
cmds = [
  "cd backend && npm install --include=dev --include=optional",
  "cd frontend && NODE_ENV=development npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
